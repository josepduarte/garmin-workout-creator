# Parser Mode Configuration
# Options: "ai" or "regex"
PARSER_MODE=regex

# AI Provider (only used when PARSER_MODE=ai)
# Options: "claude" or "openai"
AI_PROVIDER=claude

# API Keys (only required when PARSER_MODE=ai)
ANTHROPIC_API_KEY=sk-ant-your-key-here
OPENAI_API_KEY=sk-your-key-here

# Garmin Connect Credentials
# Note: Credentials are stored securely in ~/.garth after first login
# These variables are only used for initial authentication if needed
# GARMIN_EMAIL=your-email@example.com
# GARMIN_PASSWORD=your-password

# Logging
LOG_LEVEL=INFO
